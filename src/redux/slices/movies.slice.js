import {crea} from '@redux'

const initialState = {
    movies: [],
    // genres: [],
    currentMovie: null,
    currentPage: null,
    loading: false,
    nightMode: false
}

const getMovies =

const moviesSlice = createSlice({
    name: 'moviesSlice',
    initialState,
    reducers: {
    
    }
})

